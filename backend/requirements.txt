fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlmodel>=0.0.22,<1.0
pydantic>=2.8,<3.0
pydantic-settings>=2.4,<3.0
httpx>=0.27,<1.0
beautifulsoup4>=4.12,<5.0
python-dotenv>=1.0,<2.0

# Optional, heavy dependency for Milestone 8 (browser-assisted applications).
# After installing, run `playwright install chromium` to download the
# actual browser binary - a separate ~150-300MB download not covered by
# pip install. The rest of the app works fine without this.
playwright>=1.47,<2.0

# Resume export (PDF) - closes the Milestone 8 gap where autofill was
# uploading raw .txt content to resume-upload fields.
reportlab>=4.2,<5.0
pypdf>=5.0,<6.0

# Auth (multi-user accounts, JWT sessions)
pyjwt>=2.9,<3.0
bcrypt>=4.2,<5.0
email-validator>=2.2,<3.0

# dev / test
pytest>=8.3,<9.0
pytest-asyncio>=0.24,<1.0
